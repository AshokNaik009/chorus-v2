# Handoff — end of Phase <N>

Copy this to `/HANDOFF.md` at repo root when a phase ends. Overwrite the
previous one; git history keeps the old ones.

## Status
- Phase <N> complete: yes / no
- Acceptance criteria met: <x> of <y>  (list any unmet, with the reason)

## What exists now
<One paragraph. A cold session reads this to know what it is standing on.>

## Types and contracts the next phase depends on
<Actual type signatures. Not prose. The next session cannot read your mind.>

## Numbers
<Benchmark results if the phase produced or touched them. Do not round favorably.>

## Surprises
<Anything that contradicted PLAN.md. This is the most valuable section —
if a "key decision" in PLAN.md turned out wrong, say so here and amend PLAN.md.>

## Open threads deliberately left
<Things you chose not to do, so the next session does not think they were missed.>
